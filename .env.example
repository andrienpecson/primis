# Copy to .env and fill in your credentials.
# Bearer token for the REST Countries API.
API_KEY=your-api-key-here

# Optional: override the API base URL (defaults to https://api.restcountries.com).
# API_BASE_URL=https://api.restcountries.com
